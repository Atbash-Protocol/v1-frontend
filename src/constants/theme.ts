import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#919fa6',
        },
        secondary: {
            main: '#FFFFFF',
        },
        cardBackground: {
            main: 'rgba(0,1,40,.5)',
        },
    },
});
